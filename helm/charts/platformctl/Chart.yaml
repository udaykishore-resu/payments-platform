apiVersion: v2
name: platformctl
description: Migrations, post-deploy smoke tests and the scheduled operational sweeps. Jobs and CronJobs only — platformctl never runs as a long-lived service.
type: application
version: 1.0.0
appVersion: "1.0.0"
kubeVersion: ">=1.28.0-0"
annotations:
  pp.plane: ops
  pp.node-group: control
dependencies:
  - name: pp-common
    version: 1.0.0
    repository: "file://../pp-common"
